export interface Theme {
    colors: {
        100: string;
        300: string;
        500: string;
        700: string;
        900: string;
    };
};

export interface ThemeProps {
    theme: Theme;
}

